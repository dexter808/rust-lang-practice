struct Solution;

// In progress

use std::collections::BinaryHeap;

impl Solution {
    pub fn erase_overlap_intervals(intervals: Vec<Vec<i32>>) -> i32 {
        let mut a = intervals.clone();
        0
    }
}